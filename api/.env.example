########################################
# Option A (recommended, zero-install): #
########################################
# Run `npx prisma dev` from /api in a separate terminal. It starts a local
# Postgres-compatible dev server and prints a DATABASE_URL and a
# SHADOW_DATABASE_URL — copy both here. They default to these connection
# strings on this machine:
DATABASE_URL="postgresql://postgres:postgres@localhost:51214/template1?sslmode=disable&connection_limit=10&connect_timeout=0&max_idle_connection_lifetime=0&pool_timeout=0&socket_timeout=0"
SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:51215/template1?sslmode=disable&connection_limit=10&connect_timeout=0&max_idle_connection_lifetime=0&pool_timeout=0&socket_timeout=0"

########################################
# Option B: a real local/hosted Postgres #
########################################
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/wedding_automation?schema=public"
# SHADOW_DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/wedding_automation_shadow?schema=public"

PORT=4000

########################################
# Google Calendar integration           #
########################################
# From a Google Cloud Console project with the Google Calendar API enabled
# and an OAuth 2.0 Client ID (type: Web application). Add
# GOOGLE_REDIRECT_URI below as an authorized redirect URI on that client.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:4000/auth/google/callback"

# Where the browser gets sent back to after the Google OAuth flow finishes.
FRONTEND_URL="http://localhost:3000"

########################################
# Email automation (Resend)             #
########################################
# From https://resend.com — API Keys page. The free tier is enough for this.
# FROM_EMAIL must be a verified sender: either an address on a domain you've
# verified in Resend, or Resend's own onboarding@resend.dev for testing
# (only delivers to the email you signed up to Resend with).
RESEND_API_KEY=""
FROM_EMAIL="onboarding@resend.dev"
